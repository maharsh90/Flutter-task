import 'package:flutter/cupertino.dart';

class Activity {
  Color color;
  String title;
  String content;
  String goal;
  IconData icon;
  Activity(this.color, this.title, this.content, this.goal, this.icon);
}
